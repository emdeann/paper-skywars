package dev.deann.Managers;

import dev.deann.Enum.GameType;
import dev.deann.MinigameServer;

public class EggWarsGameManager extends GameManager {

    public EggWarsGameManager(MinigameServer plugin, GameType gameType) {
        super(plugin, gameType);
    }
}
